<Window x:Class="DesktopCalendar.App.MonthPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="년/월 이동" Width="240" Height="160"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="년" Margin="0,0,0,4" />
                <ComboBox Name="YearComboBox" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="월" Margin="0,0,0,4" />
                <ComboBox Name="MonthComboBox" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Name="TodayButton" Content="오늘로" Width="60" Margin="0,0,6,0" Click="TodayButton_Click" />
            <Button Name="OkButton" Content="이동" Width="60" Margin="0,0,6,0" Click="OkButton_Click" IsDefault="True" />
            <Button Name="CancelButton" Content="취소" Width="60" Click="CancelButton_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
