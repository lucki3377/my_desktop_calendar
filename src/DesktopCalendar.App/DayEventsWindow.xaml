<Window x:Class="DesktopCalendar.App.DayEventsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="일정" Width="320" Height="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Name="DateTitleText" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />

        <DockPanel Grid.Row="1" Margin="0,0,0,10" LastChildFill="False">
            <TextBlock Name="HolidayStatusText" DockPanel.Dock="Left" VerticalAlignment="Center"
                       Foreground="IndianRed" Text="공휴일 아님" />
            <Button Name="HolidayToggleButton" DockPanel.Dock="Right" Width="90" Click="HolidayToggleButton_Click" />
        </DockPanel>

        <ListBox Grid.Row="2" Name="ScheduleListBox" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Name="AddButton" Content="추가" Width="60" Margin="0,0,6,0" Click="AddButton_Click" />
            <Button Name="EditButton" Content="수정" Width="60" Margin="0,0,6,0" Click="EditButton_Click" />
            <Button Name="DeleteButton" Content="삭제" Width="60" Margin="0,0,6,0" Click="DeleteButton_Click" />
            <Button Name="CloseButton" Content="닫기" Width="60" Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>
