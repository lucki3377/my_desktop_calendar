<Window x:Class="DesktopCalendar.App.FontSizeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="글자 크기 조절" Width="320" Height="180"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Name="PercentText" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />

        <Slider Grid.Row="1" Name="ScaleSlider"
                Minimum="0.7" Maximum="2.0" TickFrequency="0.1"
                IsSnapToTickEnabled="True" SmallChange="0.1" LargeChange="0.1"
                ValueChanged="ScaleSlider_ValueChanged" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Name="ApplyButton" Content="적용" Width="70" Margin="0,0,6,0" Click="ApplyButton_Click" IsDefault="True" />
            <Button Name="CancelButton" Content="취소" Width="70" Click="CancelButton_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
