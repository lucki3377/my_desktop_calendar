<Window x:Class="DesktopCalendar.App.LunarDatePickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="음력 날짜 선택" Width="380" Height="280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Foreground="#555555"
                   Text="음력 날짜를 고르면 해당하는 양력 날짜로 일정이 등록됩니다." />

        <Grid Grid.Row="1" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="년" Margin="0,0,0,4" />
                <ComboBox Name="YearComboBox" SelectionChanged="Input_Changed" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="월" Margin="0,0,0,4" />
                <ComboBox Name="MonthComboBox" SelectionChanged="Input_Changed" />
            </StackPanel>
            <StackPanel Grid.Column="4">
                <TextBlock Text="일" Margin="0,0,0,4" />
                <ComboBox Name="DayComboBox" SelectionChanged="Input_Changed" />
            </StackPanel>
        </Grid>

        <CheckBox Grid.Row="2" Name="LeapMonthCheckBox" Content="윤달" Margin="0,12,0,0"
                  Checked="Input_Changed" Unchecked="Input_Changed" />

        <Border Grid.Row="3" Margin="0,14,0,0" Padding="12,10,12,10" CornerRadius="4"
                Background="#F2F4F7" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="양력으로는" Foreground="#666666" FontSize="11" />
                <TextBlock Name="SolarDateText" FontSize="15" FontWeight="Bold" Margin="0,4,0,0" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Name="OkButton" Content="확인" Width="70" Margin="0,0,6,0" Click="OkButton_Click" IsDefault="True" />
            <Button Name="CancelButton" Content="취소" Width="70" Click="CancelButton_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
